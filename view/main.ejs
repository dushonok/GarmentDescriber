<h1><%= title %></h1>

<% for(var i=0; i<supplies.length; i++) { %>
    <button type="button" accesskey="<%= i %>"  class="btn btn-default">
        <a href=""><%= i %></a>. <%= supplies[i].getName() %>
    </button>
<% } %>
